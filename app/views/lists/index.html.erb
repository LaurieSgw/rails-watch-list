<h1>Lists</h1>

<%= link_to "Create a new list", new_list_path(@list) %>


<div>
<% @lists.each do |list| %>
   <li> <%= link_to list.name, list_path(list) %> <%= link_to "❌", lists_path, data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}  %></li>

<% end %>
</div>
